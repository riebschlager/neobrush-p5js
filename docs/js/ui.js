const ui = new Vue({
    el: '#ui',
    data: {
        numberOfLines: 100,
        easeFactor: 0.25,
        easeFactorJitter: 0.1,
        speedFactor: 0.3,
        speedFactorJitter: 0.1,
        numberOfVertices: 8,
        numberOfVerticesJitter: 1
    }
});
